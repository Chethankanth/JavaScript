//to hold different types of data we need containers
//so we use variables.
// there are three types to define variables in js
// we can use "var","let" and "const" before the variable name

let age = 24;

var fullName = "Pushpavathi C R";

const price=99.99;

x = null;

y = undefined;

isFollow= false;

console.log(fullName);
console.log(age);
console.log(price);

console.log(y);

x
typeof(x);


/* var key word has global scope
but the const and the var key word has only
block scope*/
